{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "docker:run": "sh run-docker.sh",
    "dev": "cross-env NODE_ENV=development directus start",
    "start": "cross-env NODE_ENV=production directus start",
    "push": "git add . && git commit -m 'update' && git push origin main --force",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cross-env": "^10.0.0",
    "directus": "^11.11.0"
  }
}
